"""履歴書テンプレート（近畿高等学校統一用紙 その2・令和7年度改定）の記入位置定義。

座標はすべて「印刷したときに見える向き」（A4横 842pt × 595pt、左上が原点）で
指定する。テンプレートPDFは /Rotate 90 が付いた縦置きページなので、描画時に
render.py 側で derotation_matrix を掛けて実際のページ座標へ変換する。

数値はテンプレートPDFの罫線（ベクター）と印字済み文字の位置から実測したもの。
用紙が改定されたときはこのファイルの数値だけを直せばよい。
"""

from __future__ import annotations

# ページサイズ（見える向き）
PAGE_W = 842.0
PAGE_H = 595.0

# ---------------------------------------------------------------- 左段：本人情報
# 罫線: x=47.2(左端) / 88.3(項目名の右) / 295.1(氏名欄の右) / 379.4(住所欄の右)
_L_TEXT = 90.0       # 記入欄の左端（罫線から少し内側）
_L_RIGHT_NAME = 293.0
_L_RIGHT_ADDR = 377.0

# (x0, y0, x1, y1)
NAME_KANA = (_L_TEXT, 85.6, _L_RIGHT_NAME, 109.1)
NAME = (_L_TEXT, 110.0, _L_RIGHT_NAME, 143.8)

# 生年月日行（y 144.7-165.5）。「昭和 ・ 平成　　年　　月　　日 生（満　　歳）」が印字済み
BIRTH_ROW = (144.7, 165.5)
BIRTH_YEAR = (148.5, 161.5)    # 「年」の直前（印字済みの丸囲みを避ける）
BIRTH_MONTH = (170.0, 191.0)   # 「月」の直前
BIRTH_DAY = (201.0, 223.0)     # 「日」の直前
BIRTH_AGE = (262.0, 280.0)     # 「歳）」の直前
# 元号の丸囲み（テンプレートには「平成」に丸が印字済み）
BIRTH_ERA_CIRCLE = {
    "昭和": (93.0, 145.5, 116.0, 164.0),
    "平成": (116.3, 146.0, 148.1, 163.4),
}
BIRTH_ERA_PREPRINTED = "平成"  # テンプレートに最初から丸が付いている元号

# 現住所ブロック（ふりがな y166.4-183.5 / 本欄 y184.5-218.3、点線区切り y≒184）
ADDR_KANA = (_L_TEXT, 166.4, _L_RIGHT_ADDR, 183.5)
ADDR_ZIP = (108.0, 185.0, 185.0, 198.5)     # 印字済みの「〒」の右
ADDR_TEXT = (95.0, 198.5, _L_RIGHT_ADDR, 218.3)

# 連絡先ブロック（ふりがな y219.3-236.5 / 本欄 y237.5-267.3）
CONTACT_KANA = (_L_TEXT, 219.3, _L_RIGHT_ADDR, 236.5)
CONTACT_ZIP = (108.0, 238.0, 185.0, 251.5)
CONTACT_TEXT = (95.0, 251.5, _L_RIGHT_ADDR, 267.3)

# ---------------------------------------------------------------- 左段：職歴
# 4行。各行の上端 y と、行内の相対位置は共通。
JOB_ROWS = [(407.2, 441.6), (442.6, 476.3), (477.2, 510.2), (511.2, 547.0)]
JOB_ERA_CIRCLE = {"平成": (88.5, 3.0, 111.5, 14.2), "令和": (88.5, 20.6, 111.5, 31.8)}
JOB_YEAR = (112.0, 131.0)   # 行内 x 範囲（「年」の直前）
JOB_MONTH = (140.0, 156.0)  # 「月」の直前
JOB_YM_BAND = (13.0, 30.0)  # 行の上端からの相対 y（「年 月」の高さ）
JOB_TEXT = (169.0, _L_RIGHT_ADDR)

# ---------------------------------------------------------------- 右段
# 罫線: x=412.3(左端) / 453.5(項目名の右) / 518.2(取得年月の右) / 746.0(右端)
_R_TEXT = 456.0
_R_RIGHT = 743.0

# 資格等（見出し行の下 y73.3 から y194.0 まで）
LICENSE_AREA = (73.3, 194.0)
LICENSE_YM = (456.0, 516.0)     # 取得年月の列
LICENSE_NAME = (521.0, _R_RIGHT)  # 資格等の名称の列
LICENSE_ROWS = 5                # 既定の行数（入力が多ければ自動で詰める）

# 校内外の諸活動
ACTIVITIES = (_R_TEXT, 197.0, _R_RIGHT, 296.0)

# 志望の動機／希望の職種／アピールポイント（3項目でひとつの大きな欄）
MOTIVATION = (_R_TEXT, 301.0, _R_RIGHT, 474.0)

# 備考
REMARKS = (_R_TEXT, 479.0, _R_RIGHT, 545.0)

# ---------------------------------------------------------------- 既定の文字サイズ
FONT_SIZES = {
    "kana": 8.5,
    "name": 16.0,
    "birth": 10.5,
    "zip": 10.5,
    "address": 10.5,
    "license": 10.5,
    "body": 10.5,
    "job": 10.5,
}

# 自動縮小の下限（これより小さくなる場合は警告を出す）
MIN_FONT_SIZE = 6.5
